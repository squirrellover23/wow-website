<!DOCTYPE html>
<html>
<head>
    <title>Classes</title>
    <link rel="stylesheet" href="stylesheets/classes.css">
</head>
<body>
    <div id="header-container"></div>
    <script>
        // Fetch and insert the header using JavaScript
        fetch('header.html')
            .then(response => response.text())
            .then(html => {
                document.getElementById('header-container').innerHTML = html;
            });
    </script>
    <h1>All Classes</h1>
    <div class="container">
        <div class="table-container">
            <h2>Enrolled Classes</h2>
            <% if (registeredClasses.length > 0) { %>
                <table class="class-table">
                    <thead>
                        <tr>
                            <th>Class Name</th>
                            <th>Delete</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% registeredClasses.forEach(function(classItem) { %>
                            <tr>
                                <td><%= classItem.className %></td>
                                <td>
                                    <form action="/deleteclass" method="post" onsubmit="return confirmDelete();">
                                        <input type="hidden" name="className" value="<%= classItem.className %>">
                                        <button type="submit" class="delete-button">Delete</button>
                                    </form>
                                </td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            <% } else { %>
                <p>No Enrolled Classes.</p>
            <% } %>
        </div>

        <div class="table-container">
            <h2>Open Enrollment Classes</h2>
            <% if (unregisteredClasses.length > 0) { %>
                <table class="class-table">
                    <thead>
                        <tr>
                            <th>Class Name</th>
                            <th>Delete</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% unregisteredClasses.forEach(function(classItem) { %>
                            <tr>
                                <td><%= classItem.className %></td>
                                <td>
                                    <form action="/deleteclass" method="post" onsubmit="return confirmDelete();">
                                        <input type="hidden" name="className" value="<%= classItem.className %>">
                                        <button type="submit" class="delete-button">Delete</button>
                                    </form>
                                </td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            <% } else { %>
                <p>No Open Enrollment Classes.</p>
            <% } %>
        </div>
    </div>

    <div class="form-container">
        <form class="add-class" method="post" action="/add-class">
            <h3 id="noMargin">Add New Class</h3>
            <label for="className">Class Name:</label>
            <input type="text" id="className" name="className" required>
            <br>
            <label for="registered">Registered:</label>
            <select id="registered" name="registered" required>
                <option value="" disabled selected>Select an option</option>
                <option value="true">Enrolled</option>
                <option value="false">Open Enrollment</option>
            </select>
            <br>
            <button type="submit">Add Class</button>
        </form>
    </div>
    <br>
    <br>
</body>
<script>
    function confirmDelete() {
        return confirm('Are you sure you want to delete this class?');
    }

</script>
</html>